
function twoSum(arrays, target) {
    for (let i = 0; i < arrays.length; i++) {
        const compliment = target - arrays[i]
        console.log("compliment", compliment)
        // if(arrays.findIndex((item) => ))
    }
}


const arr = [1, 3, 5, 6]

const result = twoSum(arr, 8)